{
  "name": "scrapeverse-sentinel",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "fast-check": "^3.19.0",
    "@types/node": "^22.0.0"
  }
}
